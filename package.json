{
  "name": "@tricoteuses/juridique",
  "description": "Monorepo for projects based on Tricoteuses' Assemblee, Légifrance & Sénat",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": "=22.14.0"
  },
  "scripts": {
    "build": "npm run build --workspaces",
    "format": "npm run format --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "@types/node": "^24.3.1",
    "@typescript-eslint/eslint-plugin": "^8.43.0",
    "@typescript-eslint/parser": "^8.43.0",
    "eslint": "^9.35.0",
    "fs-extra": "^11.3.1",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  }
}
